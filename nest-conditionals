<!DOCTYPE html>
<html>
<body>

<p> Print the following to the console log: <br> <br> Print out a list of numbers from 1 - 50. If the number is divisible by 3, print "Foo" instead of the number.
If divisible by 5, print "Bar" instead of the number. If the number is divisible by both 3 and 5, print out "FooBar" in place of the number. </p>

<script>

for (var i= 1; i <= 50; i++)
    if((i % 3 === 0 ) && (i % 5 === 0)){console.log("FooBar");
    }
        else if (i % 3 === 0 ){console.log("Foo");
        }
        else if   (i % 5 === 0){console.log("Bar");
        }
    else{console.log (i);
    }


</script>

</body>
</html
